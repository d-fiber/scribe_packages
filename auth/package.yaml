name: auth
description: "What an account is, from the sign up to a device it trusts: the doors, the sessions, the pending tokens and the bans."
version: 1.0.0

environment:
  scribe: "^11.0.0"

dependencies:
  foundation: ^1.0.0
  "@scribe/contracts/": any
  "@scribe/runtime/": any
  "libphonenumber-js": any

dev_dependencies:
  "@scribe/testing/": any
  "@std/testing/time": any
  "@supabase/postgrest-js": any

scribe:
  db:
    init: ./db/init/
  protocol: ./protocol/
