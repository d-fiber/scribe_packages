name: storage
description: The objects, the buckets, and the derivatives an image is served as.
version: 1.0.0

environment:
  scribe: "^9.0.0"

dependencies:
  foundation: ^1.0.0

scribe:
  db:
    init: ./db/init/
  protocol: ./protocol/
  ops:
    - ./ops/
