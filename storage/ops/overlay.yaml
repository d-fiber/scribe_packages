services:
  db:
    environment:
      STORAGE_ADMIN_PASSWORD: "${STORAGE_ADMIN_PASSWORD}"
    volumes:
      - "{{sdk_root}}/packages/storage/db/provisioning/roles.sql:/docker-entrypoint-initdb.d/init-scripts/98-storage-roles.sql:Z"
