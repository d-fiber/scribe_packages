name: remote_configs
description: The keys a project names in code, each with its default and its lifetime.
version: 1.0.0

environment:
  scribe: "^1.0.0"

dependencies:
  foundation: ^1.0.0

dev_dependencies:
