name: realtime
description: The life of a row, broadcast to the callers a channel lets in.
version: 1.0.0

environment:
  scribe: "^1.0.0"

dependencies:
  foundation: ^1.0.0

dev_dependencies:
  "@scribe/testing/": any
  "@supabase/postgrest-js": any

scribe:
  db:
    init: ./deploy/db/init/
    provisioning: ./deploy/db/provisioning/
  protocol: ./protocol/
  services:
    - ./deploy/services/realtime/
