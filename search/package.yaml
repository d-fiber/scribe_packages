name: search
description: The full text index, what it holds, and the way it is queried.
version: 1.0.0

environment:
  scribe: "^1.0.0"

dependencies:
  foundation: ^1.0.0
  "@opensearch-project/opensearch": any
  "@opensearch-project/opensearch/": any
  "@scribe/runtime/": any

dev_dependencies:
  "@scribe/testing/": any
  "@supabase/postgrest-js": any

scribe:
  db:
    init: ./db/init/
  protocol: ./protocol/
  ops:
    - ./ops/
