name: search
description: The full text index, what it holds, and the way it is queried.
version: 1.0.0

environment:
  scribe: "^9.0.0"

dependencies:
  foundation: ^1.0.0

scribe:
  db:
    init: ./db/init/
  protocol: ./protocol/
  ops:
    - ./ops/
