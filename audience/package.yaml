name: audience
description: The named set somebody belongs to, and the right that follows from it.
version: 1.0.0

environment:
  scribe: "^9.0.0"

dependencies:
  foundation: ^1.0.0

scribe:
  db:
    init: ./db/init/
  ops:
    - ./ops/
